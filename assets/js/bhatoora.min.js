$(window).load(function() {
	// $(".loader").fadeOut("slow");
})

$(document).ready(function() {

    var loginInput = $('.login_field > input');

    loginInput.on({
    	focus: function() {
            loginInputValue = $(this).val(),
	    	loginInputlabel = $(this).siblings('label');
	    	loginInputlabel.css('display', 'none')
        },
        blur: function() {
        	loginInputValue = $(this).val(),
	    	loginInputlabel = $(this).siblings('label');

        	if (loginInputValue == '') {
        		loginInputlabel.css('display', 'block');
	    	} else {
	    		loginInputlabel.css('display', 'none');
	    	}
        }
    })

    $('.has_sub').hover(function(){
		$(this).find('.sub').stop().slideDown('slow');
		// $(this).children('a').addClass('active');
	}, function(){
		$(this).find('.sub').stop().slideUp('slow');
		// $(this).find('a').removeClass('active');
	});

    var srchInput = $('#srch > input');

    srchInput.on({
    	focus: function() {
            srchInputValue = $(this).val(),
	    	srchInputlabel = $(this).siblings('label');
	    	srchInputlabel.css('display', 'none')
        },
        blur: function() {
        	srchInputValue = $(this).val(),
	    	srchInputlabel = $(this).siblings('label');

        	if (srchInputValue == '') {
        		srchInputlabel.css('display', 'block');
	    	} else {
	    		srchInputlabel.css('display', 'none');
	    	}
        }
    })
    

});